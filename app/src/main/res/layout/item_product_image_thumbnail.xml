<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/thumbnail_container"
    android:layout_width="80dp"
    android:layout_height="80dp"
    android:layout_marginEnd="8dp"
    android:padding="4dp">

    <FrameLayout
        android:id="@+id/thumbnail_frame"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="bottom|start"
        android:background="@drawable/bg_product_thumbnail_unselected"
        android:padding="3dp">

        <ImageView
            android:id="@+id/img_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/product_image_thumbnail"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_product" />
    </FrameLayout>

    <TextView
        android:id="@+id/btn_remove_image"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:background="@drawable/bg_image_remove_button"
        android:contentDescription="Xóa ảnh"
        android:elevation="4dp"
        android:gravity="center"
        android:text="X"
        android:textColor="@color/white"
        android:textSize="9sp"
        android:textStyle="bold"
        android:visibility="gone" />
</FrameLayout>
